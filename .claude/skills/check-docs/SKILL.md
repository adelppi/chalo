---
name: check-docs
description: docs/ の整合性を検査する。リンク切れ・「一次情報は1か所」違反・目次と実ファイルの乖離・未確定記述と open-questions.md の同期・features.md の実装状況とコードの乖離を検出して報告する。
---

`docs/` 全体の整合性を検査し、結果を報告する。**修正はユーザーの承認を得てから**行う。

## 検査項目

1. **リンク・参照**: docs 内の相対リンクや `adr/XXXX`・`domain/xxx.md` 形式の参照が、実在するファイルを指しているか。
2. **目次との一致**: `docs/README.md`「どこを読むか」「一次情報は1か所だけ」の表が指す文書・パスが実在するか。`docs/` にあるのに README のどの行からも辿れない文書はないか。
3. **一次情報の重複**: `docs/README.md`「一次情報は1か所だけ」の表に載っている事実が、正とされる文書以外で再定義(リンクではなく再掲)されていないか。
4. **未確定記述の同期**: 本文で「未確定」としている記述が `docs/open-questions.md` に Q-ID 付きで登録されているか(Q-ID を持たない未確定記述、および実在しない Q-ID への参照がないか)。逆に、解決済みの論点が本文に未確定のまま残っていないか。
5. **ADR の健全性**: 採番が連続しているか、各 ADR の `関連:` の参照先が実在するか、廃止(superseded)された ADR を正として参照している箇所がないか。
6. **コードとの乖離**(`src/` が存在する場合のみ):
   - adr/0015 の依存ルール違反(feature 間の直接 import、`global/lib/supabase` 以外での supabase-js の import)がないか。
   - `src/features/` の一覧と adr/0015 の feature 割り付け・`docs/features.md` が整合しているか。
   - **実装状況の整合**: `docs/features.md`「実装済み」に載っている機能のコードが実在するか。逆に「未実装」の機能が実装済みになっていないか(実装したのに一覧を移し忘れていないか)。
   - `docs/data-model.md` の設計判断が `app/supabase/migrations/` の実スキーマと矛盾していないか(列の一覧は data-model.md には無い。migrations が正)。

## 報告形式

- 問題を「**重要**(事実の矛盾・リンク切れ・ルール違反)」と「**軽微**(表記ゆれ・目次の抜け)」に分けて列挙する。
- それぞれ該当ファイルと行、修正案を添える。
- 問題がなければ「問題なし」と明確に報告する。検査した項目の一覧も添える。
