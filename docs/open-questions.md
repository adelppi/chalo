# 未確定論点・決定バックログ — chalo

各ファイルに散らばる `[保留]` / `確定させたい論点` を1か所に集約したハブ。
ここで扱うのは**機能の有無ではなく、値・形式・方式といった設計の詳細**。機能を持つ／持たないは `features.md` で確定済み。

## 使い方

- 各論点には ID（`Q-<領域>-<番号>`）を振る。各文書の本文からはこの ID で参照してよい。
- 決まったら結論を**該当する元ファイルへ反映**し、この文書からは論点ごと削除する（決定の記録は元ファイルと git 履歴が持つ。ここに決定を再掲しない）。
- 優先度は付けず、領域別に並べる。

**状態**：`未着手` / `議論中` / `暫定案あり` / `保留` / `提案`

---

## 論点インデックス（未決）

| ID | 論点 | 状態 |
|---|---|---|
| [Q-ONB-1](#q-onb-1) | 起動時 pairState 取得失敗（オフライン等）時のフォールバック | 暫定案あり |
| [Q-NOTIF-2](#q-notif-2) | 振り返り通知・同日複数該当の扱い | 暫定案あり |
| [Q-FEAT-1](#q-feat-1) | 一覧の並び替え・絞り込みを持つか | 未着手 |
| [Q-OPS-1](#q-ops-1) | クラッシュ/エラー監視（Sentry 等）を入れるか | 未着手 |
| [Q-BIZ-1](#q-biz-1) | 収益化（プレミアム余地） | 保留 |
| [Q-KPI-1](#q-kpi-1) | 成功指標（KPI）の確定 | 提案 |

> いずれもリリース済み機能をブロックしない。Q-NOTIF-2・Q-FEAT-1・Q-OPS-1 は該当機能の実装時に確定する（`features.md`「未実装」）。

---

## オンボーディング（Q-ONB）

### Q-ONB-1
**起動時 pairState 取得失敗（オフライン等）時のフォールバック**
- 内容：起動ゲートはオンボーディングの要否を決める前に、サーバの pairState（`profiles.pair_id` の有無）の取得を待つ（Issue #56）。取得が失敗・停止（オフライン等）した場合、(a) ローカル進捗に従ってオンボーディングを表示する（＝pairState 抜きで判定）か、(b) 前回取得できたペア状態を端末に保持しておき、それを使うか。
- 暫定案：(a) を実装。既にペア済みだがローカル完了フラグが無い端末（招待コード成立前の旧バージョンでペアが成立した端末・再インストール等）がオフラインで起動すると、まれにオンボーディングが再表示されうる（オンライン復帰後の起動では発生しない）。
- 関連：`domain/onboarding.md`（起動時の着地先・優先順位1）
- 状態：暫定案あり
- 結論：

---

## 通知（Q-NOTIF）

### Q-NOTIF-2
**振り返り通知・同日複数該当の扱い**
- 内容：同じ通知日に複数プランが該当したとき、「まとめて1通（タップでリスト）」にするか「プランごとに送る（同朝に複数届くのを許容）」か。「1日1通まで」上限との整合。
- 暫定案：頻度が低いため「プランごとに送る」を許容する案を推奨。
- 関連：`domain/notifications.md`（同日に複数該当した場合）、`adr/0007-notifications-architecture.md`（確定させたい論点）
- 状態：暫定案あり
- 結論：

---

## 機能スコープ（Q-FEAT）

### Q-FEAT-1
**一覧の並び替え・絞り込みを持つか**
- 内容：並び替え/絞り込みを提供するか、初期は既定順のみか。
- 暫定案：なし（現状は既定順のみ）。
- 関連：`features.md`「未実装」2.4
- 状態：未着手
- 結論：

---

## 運用・監視（Q-OPS）

### Q-OPS-1
**クラッシュ/エラー監視（Sentry 等）を入れるか**
- 内容：クラッシュ監視ツールを導入するか。入れる場合もプライバシー配慮。
- 暫定案：なし（現状は端末内ログ＋不具合報告で拾う）。
- 関連：`non-functional.md`（ログ/可観測性）、`features.md`「未実装」11.6、`adr/0011-logging.md`
- 状態：未着手
- 結論：

---

## ビジネス・指標（Q-BIZ / Q-KPI）

### Q-BIZ-1
**収益化（プレミアム余地）**
- 内容：プレミアムの余地（書き出し拡張等）。無料を前提に設計し、設計を縛らない。
- 暫定案：メモのみ。課金の有無・内容は別途検討。
- 関連：`features.md`（収益化）、`non-functional.md`（対応範囲）
- 状態：保留
- 結論：

### Q-KPI-1
**成功指標（KPI）の確定**
- 内容：厳密な数値目標は置かないが、KPI 候補（プランを貯め続けるか／アルバムを開くか／振り返り通知から再来訪するか）を確定する。
- 暫定案：上記3点を候補とする。
- 関連：`overview.md`（成功の見方 [提案]）
- 状態：提案
- 結論：
